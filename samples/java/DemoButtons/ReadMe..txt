The batch files can be used to compile and run the sample

They assume the SDK is installed in "C:\Wacom STU SDK" to locate the required jar file

If not, change the value:
@set INSTALL_PATH="\Wacom STU SDK"

Use Run-DemoButtons-64.bat for a 64 bit version.



